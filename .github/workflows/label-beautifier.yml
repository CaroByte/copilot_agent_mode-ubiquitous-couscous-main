name: 🏷️✨ Label Beautifier

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run mode (only show changes, do not apply them)'
        required: true
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  beautify:
    uses: CaroByte/agentic-repo-workflows/.github/workflows/label-beautifier.yml@main
    with:
      dry_run: ${{ inputs.dry_run }}
    secrets: inherit
